package main;

func foo() {
    return;
}

func add(a int, b int) int {
    return a + b;
}

func main() {
    var x int = 5;
    var y int = 10;

    x = 42;
    x = y;
    x = (y + 5);

    foo();
    add(x, y);
    add(x + 1, y * 2);

    return;
}